def func(n, step):
	i = 0
	number = []

	while i < n:
		print "At the top i is %d" % i
		number.append(i)

		i = i + step
		print "Number now: ", number
		print "At the bottom i is %d" % i

	print "The numbers: "
	
	for num in number:
		print num

func(125,5)